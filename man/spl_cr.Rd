% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spl_csh.R
\name{spl_csh}
\alias{spl_csh}
\title{Smoothed Predictive Likelihood for Cause-Specific Hazards (SPL-CR)}
\usage{
spl_csh(
  formula,
  data,
  time = "time",
  status = "status",
  causes = NULL,
  folds = 10L,
  k_nn = 5L,
  kernel = "epanechnikov",
  seed = NULL,
  stratify = TRUE,
  fold_id = NULL,
  return_scores = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{formula}{A model formula. Recommended format is a right-hand side only formula
such as \code{~ x1 + x2}. If a full formula is supplied, only the right-hand side
is used.}

\item{data}{A data.frame containing at least \code{time} and \code{status}.}

\item{time}{Name of the follow-up time column.}

\item{status}{Name of the event indicator column (0=censoring; positive integers are causes).}

\item{causes}{Integer vector of causes to include. Default uses all positive values in \code{status}.}

\item{folds}{Number of CV folds.}

\item{k_nn}{Number of nearest neighbours for kNN bandwidth.}

\item{kernel}{Kernel name or id (uniform, triangular, epanechnikov, quartic).}

\item{seed}{Optional integer seed to generate folds. If NULL, no seed is set.}

\item{stratify}{Whether to stratify fold assignment by \code{status}.}

\item{fold_id}{Optional precomputed fold assignment (integer vector length nrow(data)).
If provided, \code{seed} and \code{stratify} are ignored.}

\item{return_scores}{Whether to return per-observation out-of-fold scores.}

\item{verbose}{Whether to print fold progress.}
}
\value{
An object of class \code{splcr_result}.
}
\description{
Computes cross-validated smoothed predictive log-scores under the cause-specific
hazards representation by smoothing baseline cumulative hazard jumps on each
training fold using a k-nearest-neighbour kernel smoother.
}
\details{
This function is implemented to be numerically consistent with the reference
analysis scripts used in the accompanying manuscript.

Time-unit note:
\itemize{
  \item This function is unit-agnostic (days / years / months are all allowed).
  \item Absolute log-scores depend on the time unit because event terms involve a hazard density.
  \item For fair model comparison, use the same time unit across all competing models.
}
}
