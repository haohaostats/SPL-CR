% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spl_compare_wide.R
\name{spl_compare_wide}
\alias{spl_compare_wide}
\title{Convert `spl_compare()` output to a manuscript-style wide table}
\usage{
spl_compare_wide(
  x,
  value_col = "Lhat",
  digits = 4L,
  kernel_order = c("Uniform", "Triangular", "Epanechnikov", "Quartic")
)
}
\arguments{
\item{x}{A data.frame returned by \code{spl_compare()}.}

\item{value_col}{Character scalar. Which column to spread across kernels.
Default is \code{"Lhat"}. You may also use \code{"se_L"} if present.}

\item{digits}{Integer. Number of digits for rounding numeric values. Default is 4.}

\item{kernel_order}{Character vector specifying kernel column order in the output.
Defaults to \code{c("Uniform","Triangular","Epanechnikov","Quartic")}.}
}
\value{
An object of class \code{splcr_compare_wide} (inherits from data.frame).
}
\description{
Takes the long-format data frame returned by \code{spl_compare()} and reshapes it
into a wide table with one row per model and one column per kernel
(e.g., Uniform, Triangular, Epanechnikov, Quartic), matching common manuscript
presentation style.
}
